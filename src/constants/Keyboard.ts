export enum KeyboardAction {
  SET_INPUT,
  SHIFT,
  SYMBOL,
  LANGUAGE_CHANGE,
  SET_LAYOUT,
  SPACE,
  CLEAN,
  DELETE,
}

export enum KeyboardMode {
  NUMERIC,
  NUMERIC_DOT,
  QWERTY,
  QWERTY_NUMPAD,
}

export enum KeyboardInputType {
  OUTLINED,
  MOBILE_UNDERSCORE,
  UNDERSCORE,
  PASSWORD,
}
